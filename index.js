
/**
 * Module dependencies.
 */

var _stream = require('stream')
  , ReadableStream = _stream.Readable;

/**
 * Expose `stream`.
 */

module.exports = stream;

/**
 * A one-liner.
 *
 * @param {String} name
 * @return {Function}
 * @api public
 */

function stream(name) {

}
